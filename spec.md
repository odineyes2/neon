# neon
### 3D 사이버펑크 구룡성채 건설·경영 시뮬레이션 — 요구사항 명세 v0.1
> 이 문서는 Claude Code가 원격 저장소에서 구현을 시작할 수 있는 수준의 명세를 목표로 한다.
> 결정된 사항은 **[확정]**, 열려 있는 사항은 **[미결]** 로 표기한다.
---
## 1. 한 줄 정의
허가받지 않은 땅에 컨테이너 하나를 내려놓고, 지지되지 않는 증축과 막힌 통로와 빛이 들지 않는 방들을 버텨가며, 스스로를 도시라고 부르게 되는 수직 슬럼을 짓는 게임.
**게임이 던지는 질문:** 효율을 위해 안쪽을 채울 것인가, 사람이 살 수 있게 빛길을 남길 것인가.
---
## 2. 핵심 설계 원칙
이 세 가지를 어기는 기능은 넣지 않는다.
1. **밀집은 이득이자 병이다.** 붙여 지으면 수입이 오르지만 채광·환기·통행이 죽는다. 모든 확장 결정은 이 저울 위에 있다.
2. **건물이 곧 서사다.** 스토리 이벤트는 랜덤으로 뜨지 않는다. 플레이어가 지은 구조(암흑 셀 수, 붕괴 이력, 고립된 층)가 조건이 되어 열린다.
3. **철거는 아프다.** 위층이 얹혀 있으면 못 부순다. 잘못 지은 구조와 오래 같이 산다. 그게 구룡성채다.
---
## 3. 공간 모델 **[확정]**
### 3.1 격자
- 3D 정수 격자. 좌표 `(x, y, z)` — `x` 폭, `z` 깊이, `y` 층.
- 셀 한 칸 = 월드 유닛 `1.0 × 1.2 × 1.0` (가로 × 높이 × 세로). 층고를 1.2로 두어 실루엣이 세로로 길어지게 한다.
- 시작 부지 `3 × 3`, 최대 `13 × 13`. 확장은 사방 동시 +1링, 비용은 `800 × 2^n`.
- 최대 높이 24층. 그 이상은 코어 건설 후 해금 **[미결]**.
### 3.2 배치 제약 — 이 게임의 규칙 대부분이 여기 있다
| 제약 | 내용 |
|---|---|
| **지지 (Support)** | 셀은 바로 아래 셀이 있거나, 인접 셀에 의해 캔틸레버로 1칸까지 버틸 수 있다. 2칸 이상 돌출은 불가. |
| **하중 (Load)** | 각 블록에 `weight`. 지상까지 이어지는 기둥 라인의 누적 하중이 `structuralCapacity`를 넘으면 **경고 → 3일 후 붕괴**. 보강재(강재 기둥, H빔)로 용량을 올린다. |
| **접근 (Access)** | 모든 블록은 계단·사다리·복도를 통해 1층 출입구까지 경로가 이어져야 가동된다. 끊기면 `ISOLATED` 상태 → 생산 0, 인구 이탈. |
| **동선 거리 (Walk cost)** | 출입구까지의 경로 길이가 길수록 상업 수입과 매력에 감쇠. 계단만으로 12층 이상이면 심각한 페널티 → 엘리베이터 해금 동기. |
| **채광 (Light)** | 셀의 외기 접촉면 수(측면 4 + 옥상 1)를 계산. `0`이면 암흑 코어. |
| **환기 (Air)** | 채광과 별개로, 외기 또는 환기 덕트 네트워크에 연결되어야 한다. 미연결 시 오염이 국소 누적. |
### 3.3 채광이 만드는 배치 퍼즐 — 핵심 재미
블록마다 `lightPref`를 둔다.
- `NEEDS_LIGHT` — 주거, 농장, 진료소. 암흑 셀에 두면 매력 급락, 인구 이탈.
- `INDIFFERENT` — 계단, 창고, 배관.
- `PREFERS_DARK` — 서버 팜, 암시장, 해커 부스, 밀조장. 암흑 코어에서 오히려 보너스.
→ 플레이어는 자연스럽게 **바깥 껍질에는 주거, 안쪽 코어에는 지하경제**를 배치하게 된다. 이게 실제 구룡성채의 단면 구조이고, 별도 튜토리얼 없이 규칙만으로 유도된다.
`lightwell`(광정) 블록: 셀을 비운 채 수직으로 관통시켜 주변 셀에 채광을 공급. 공간을 버려서 삶의 질을 사는 선택지.
---
## 4. 시뮬레이션 시스템
### 4.1 자원
| 자원 | 성격 | v1 처리 | v2 처리 |
|---|---|---|---|
| 크레딧 | 저량 | 총량 | 동일 |
| 전력 | 유량 | 총량 수급비 | **배선 네트워크 연결성** |
| 물 | 유량 | 총량 수급비 | **배관 네트워크 + 층별 수압 감쇠** |
| 하수 | 유량 | 없음 | 미처리 시 오염 급증 |
| 데이터 | 유량 | 없음 | 상업 티어 4 이상 요구 |
**[확정]** v1은 총량, v2에서 네트워크로 전환. 데이터 모델은 처음부터 네트워크를 전제로 설계해 마이그레이션 비용을 없앤다.
### 4.2 파생 지표
```
오염   = Σ 블록 오염 + 국소 미환기 누적 − 농장/필터 정화
소음   = Σ 블록 소음, 인접 3셀까지 감쇠 전파 (주거에만 페널티)
질서   = 50 + Σ 블록질서 − 인구압 − 오염 − 암흑셀비율×k
매력   = 기본 + 시설 − 오염 − 소음 − 평균동선비용 + 채광충족률
수용력 = Σ 주거용량 × f(매력)
인구   = 수용력을 향해 점진 수렴 (급락은 즉시, 유입은 느리게)
```
**비대칭 규칙 [확정]:** 인구는 천천히 들어오고 빠르게 나간다. 실패가 즉각 체감되도록.
### 4.3 인접 시너지
블록 쌍 매트릭스로 정의. 예시:
- 국수집 ↔ 노점: 양쪽 수입 +15% (골목 상권)
- 나이트클럽 → 인접 주거: 매력 −12 (소음)
- 진료소 ↔ 주거: 주거 매력 +6
- 서버 팜 ↔ 서버 팜: 발열 누적, 냉각 요구 증가
- 사당 → 반경 2셀 주거: 질서 +4
### 4.4 시간
- 1틱 = 게임 내 1시간, 실시간 1.2초 (배속 0/1/2/4).
- 30일 = 1분기. 분기마다 **정산 리포트**(수지, 인구 변동, 사건 요약) 모달.
- 낮/밤 사이클이 렌더링에 직결 — 밤에 네온과 창문 불빛이 켜지고, 정전이면 꺼진다. **정전을 눈으로 본다.**
---
## 5. 3D 렌더링 **[확정: 조합형 프리팹 + 자유 궤도 카메라]**
### 5.1 블록 비주얼 구성
각 블록은 단일 메시가 아니라 **베이스 + 데코 슬롯**의 조합으로 생성한다. 이 시스템이 이 게임 비주얼의 전부다.
```
BlockVisual
├─ base        : 셸 지오메트리 (컨테이너/콘크리트/철골 중 1)
├─ facade[]    : 외기 접촉면마다 생성 — 창, 셔터, 벽돌 패턴
└─ props[]     : 절차적 배치
   ├─ 에어컨 실외기   (외벽, 무작위 1~3, 미세 회전)
   ├─ 빨랫줄        (인접 셀과 외벽 공유 시 두 셀을 잇는 라인)
   ├─ 간판/네온      (상업 블록, 외벽, 발광 머티리얼)
   ├─ 배관/전선다발   (수직 이웃이 있으면 층간 연결)
   ├─ 위성 안테나     (옥상 셀)
   ├─ 물탱크/새장     (옥상 셀)
   └─ 화분/잡동사니   (통로·계단)
```
**절차적 규칙:**
- 프롭 배치는 셀 좌표 기반 시드 해시로 결정 → 저장 불필요, 항상 동일 재현.
- 외기 접촉면에만 프롭이 붙는다. 안쪽 셀은 자동으로 민짜가 되어 **암흑 코어가 시각적으로도 드러난다.**
- 빨랫줄·배관은 이웃 셀 존재 여부를 보고 런타임 생성 → 지을수록 건물이 유기적으로 엉킨다.
### 5.2 성능 전략 — 필수
24층 × 13 × 13 = 최대 약 4,000셀. 셀당 프롭 5개면 20,000 오브젝트.
- 모든 반복 프롭은 **`InstancedMesh` 단일 드로우콜**로 통합. 프롭 타입별 인스턴스 풀 관리.
- 베이스 셸도 머티리얼별 인스턴싱.
- 블록 추가/제거 시 전체 재빌드 금지 — **더티 셀만 인스턴스 매트릭스 갱신.**
- 목표: 드로우콜 100 이하, 모바일 30fps / 데스크톱 60fps.
- 그림자는 방향광 1개 캐스케이드만. 포인트 라이트는 그림자 없음, 네온은 이미시브 + 블룸으로 처리.
### 5.3 카메라
- **OrbitControls 기반 자유 궤도.** 회전, 줌, 팬. 상하 각도는 −5°~80°로 제한(지면 아래 진입 금지).
- **층 슬라이스 [필수]**: 슬라이더로 `y > n`인 층을 숨긴다. 안쪽 셀을 편집하려면 반드시 필요.
- **X-ray 토글**: 외곽 셸을 반투명화하고 내부 셀의 채광 상태를 색으로 표시(밝음=시안, 암흑=자홍).
- **오버레이 모드**: 채광 / 접근성 / 하중 / 소음 / 오염 — 각각 셀을 히트맵으로 착색. **건설 게임의 재미는 이 오버레이에서 나온다.**
### 5.4 배치 인터랙션
- 레이캐스트로 기존 셀의 **면(face)** 을 맞히면 그 면에 인접한 빈 셀이 고스트로 표시된다.
- 고스트는 유효/무효를 색으로 구분(시안/적색), 무효 사유를 툴팁으로 표시("아래층 없음", "하중 초과").
- 빈 지면은 1층 배치.
- 모바일: 탭으로 선택 → 확인 버튼(오조작 방지). 데스크톱: 클릭 즉시 배치, 드래그로 연속 배치.
### 5.5 아트 디렉션
- 팔레트: 젖은 콘크리트 회록(#1A2422), 녹슨 컨테이너 적갈(#8A4B2A), 네온 시안(#42E8DC), 네온 자홍(#FF3D86), 나트륨등 호박(#F5A623).
- 톤 매핑 ACES, 약한 블룸, 화면 하단에 옅은 스모그 포그.
- 밤 프리셋이 기본 프레젠테이션. 낮은 색이 빠지고 회색빛으로 — 낮의 성채는 초라해 보여야 한다.
---
## 6. 콘텐츠 시스템
### 6.1 블록 티어
| 티어 | 명칭 | 해금 조건 | 대표 블록 |
|---|---|---|---|
| 0 | 무허가 정착 | 시작 | 컨테이너 주거, 노점, 디젤 발전기, 물탱크, 철제 계단 |
| 1 | 골목 형성 | 인구 12 | 국수집, 캡슐 여인숙, 정수 필터, 전당포, 광정 |
| 2 | 수직 도시 | 인구 28 | 해커 부스, 진료소, 태양광, 아케이드, 자경단 초소, H빔 보강 |
| 3 | 성채 | 인구 55 | 증축 공동주택, 암시장, 옥상 농장, 서버 팜, 화물 엘리베이터 |
| 4 | 자치 구역 | 인구 95 | 크롬 시술소, 나이트클럽, 소형 융합로, 골목 사당, 환기 덕트 |
| 5 | 메가빌딩 | 인구 170 + 13층 | 메가빌딩 코어 |
각 블록 정의는 JSON으로 분리(§8.2). 밸런스는 코드 수정 없이 조정 가능해야 한다.
### 6.2 캐릭터 **[확장]**
기존 6인(라오 첸, 아메이, 링크, 닥터 웡, 카게, 세라핌)을 유지하되 3D 전환에 맞춰 확장한다.
- **거주 셀을 가진다.** 합류 시 특정 블록에 배정되고, 3D 뷰에서 해당 셀에 아이콘이 뜬다.
- 그 셀이 철거·붕괴되거나 `ISOLATED` 상태가 되면 **캐릭터가 떠난다**(패시브 상실 + 전용 이탈 이벤트).
- 각자 **개인 퀘스트 라인** 2~3단계. 예: 링크 — 의뢰 수락 → 추적당함 → 기업 습격 방어전.
- 신규 후보: 구조 기술자(하중 용량 +20%), 배관공(수압 감쇠 완화), 밀항 브로커(인구 유입 가속).
### 6.3 스토리 이벤트
**조건은 구조에서 나온다.** 이게 다른 건설 게임과의 차별점이다.
```
암흑 셀 20개 이상          → "빛이 들지 않는 층"
붕괴 1회 이상 + 인구 40    → "무너진 자리"
15층 이상 + 엘리베이터 없음 → "노인의 계단"
평균 동선비용 상위 임계     → "길을 잃는 사람들"
채광 충족률 90% + 인구 60   → "성채의 낮"
```
이벤트 스키마는 조건 DSL + 분기 선택지 + 영구 플래그. 선택 결과가 이후 이벤트의 전제조건이 되어 사슬을 만든다.
### 6.4 승리와 그 이후
- 1차 목표: 메가빌딩 코어 건설.
- 코어 이후는 **엔드리스 + 도전 과제**(인구 500, 무붕괴 100일, 암흑 셀 0 유지 등).
- 엔딩은 플레이 스타일에 따라 분기 **[미결]**: 기업 매각 / 자치 선언 / 강제 철거 저항.
---
## 7. 기술 스택 **[확정]**
```
Vite + TypeScript
three.js (r160+)
zustand           — 상태 관리 (게임 상태와 렌더 분리)
게임 로직은 three.js 비의존 순수 TS — 헤드리스 테스트 가능해야 한다
Vitest            — 시뮬레이션 규칙 단위 테스트
```
**[미결]** UI 레이어를 React로 갈지 순수 DOM으로 갈지. React 권장(모달·패널 복잡도 고려).
### 7.1 저장소 구조
```
src/
  sim/                 # three.js 미의존. 이 폴더만으로 게임이 돌아가야 한다
    grid.ts            # 3D 격자, 좌표 유틸
    structure.ts       # 지지·하중·붕괴 판정
    access.ts          # BFS 경로 탐색, 고립 판정, 동선비용
    light.ts           # 외기 접촉면·채광 계산
    economy.ts         # 자원 수지, 인구 수렴
    adjacency.ts       # 시너지 매트릭스
    tick.ts            # 틱 오케스트레이션
    save.ts            # 직렬화 + 버전 마이그레이션
  render/
    scene.ts           # 씬, 조명, 포스트프로세싱
    camera.ts          # 궤도 컨트롤, 층 슬라이스
    blockFactory.ts    # 베이스 + 프롭 조합 생성
    instancing.ts      # InstancedMesh 풀 관리
    overlays.ts        # 히트맵 오버레이
    daynight.ts
  ui/
  data/
    blocks.json
    characters.json
    events.json
    adjacency.json
  main.ts
tests/
```
**아키텍처 원칙 [확정]:** `sim/`은 `render/`를 절대 import 하지 않는다. 렌더는 상태를 구독해 그릴 뿐이다.
---
## 8. 데이터 스키마
### 8.1 게임 상태
```ts
interface GameState {
  version: number;
  tick: number; day: number; hour: number;
  credits: number;
  bounds: { x: number; z: number };      // 부지 크기
  cells: Record<CellKey, CellData>;      // "x,y,z"
  population: number;
  characters: string[];
  eventFlags: string[];
  modifiers: Modifiers;                  // 이벤트 선택의 영구 효과
  history: { collapses: number; blackouts: number };
}
interface CellData {
  blockId: string;
  builtOn: number;                       // 건설 틱
  condition: number;                     // 0~1, 노후도
  status: 'OK' | 'ISOLATED' | 'OVERLOADED' | 'UNPOWERED';
}
```
### 8.2 블록 정의
```json
{
  "id": "noodle_shop",
  "name": "국수집",
  "tier": 1,
  "category": "commerce",
  "cost": 210,
  "upkeep": 4,
  "weight": 3,
  "structuralCapacity": 0,
  "lightPref": "NEEDS_LIGHT",
  "roofOnly": false,
  "flows": { "power": -3, "water": -4 },
  "provides": { "jobs": 3, "income": 16, "appeal": 4 },
  "emits": { "pollution": 1, "noise": 2 },
  "visual": {
    "base": "concrete_shell",
    "facade": "shutter_front",
    "props": ["neon_sign", "ac_unit", "steam_vent"]
  },
  "description": "성채의 심장. 새벽 3시에도 연다."
}
```
### 8.3 이벤트 정의
```json
{
  "id": "dark_floors",
  "trigger": { "all": [
    { "stat": "darkCells", "op": ">=", "value": 20 },
    { "stat": "population", "op": ">=", "value": 40 }
  ]},
  "once": true,
  "title": "빛이 들지 않는 층",
  "speaker": "amei",
  "body": "...",
  "choices": [
    { "text": "광정을 뚫는다",
      "cost": { "credits": 600 },
      "effects": { "appeal": 12 },
      "setFlag": "chose_lightwell" },
    { "text": "그대로 둔다",
      "effects": { "order": -10 },
      "setFlag": "chose_dark" }
  ]
}
```
---
## 9. 마일스톤
각 단계는 그 자체로 실행 가능해야 한다.
| M | 목표 | 완료 기준 |
|---|---|---|
| **M0** | 프로젝트 셋업 | Vite + TS + three.js 빌드, 빈 씬에 궤도 카메라와 지면 격자 |
| **M1** | 배치 코어 | 면 레이캐스트 → 고스트 → 배치/철거. 지지 규칙 적용. 인스턴싱된 단색 박스 |
| **M2** | 시뮬 코어 | `sim/` 완성 + 단위 테스트. 자원·인구·접근성·채광 계산. UI는 숫자만 |
| **M3** | 비주얼 | 프리팹 조합 시스템, 프롭 절차 배치, 밤낮, 네온, 블룸. **여기서 게임이 게임처럼 보인다** |
| **M4** | 오버레이·UX | 층 슬라이스, X-ray, 5종 히트맵 오버레이, 툴팁, 모바일 조작 |
| **M5** | 콘텐츠 | 블록 24종, 캐릭터 8인, 이벤트 20종, 티어 해금 |
| **M6** | 마감 | 세이브/마이그레이션, 밸런스 패스, 성능 프로파일링, 온보딩 |
**M2를 M3보다 먼저 하는 이유:** 시뮬이 재미없으면 아무리 예뻐도 게임이 아니다. M2 시점에 숫자만 보고도 "한 판 더" 소리가 나와야 다음으로 간다.
---
## 10. 품질 기준
- **성능**: 4,000셀 상태에서 드로우콜 100 이하, 데스크톱 60fps.
- **접근성**: 키보드로 전 기능 조작 가능, 포커스 링 유지, `prefers-reduced-motion` 존중, 색상만으로 정보를 전달하지 않음(오버레이에 패턴 병행).
- **저장**: 자동 저장 20초 간격 + 수동. `version` 필드 기반 마이그레이션 함수 체인.
- **테스트**: `sim/`의 지지·붕괴·고립·채광 판정은 단위 테스트 필수. 렌더는 제외.
---
## 11. 열린 질문 **[미결]**
1. 최대 높이 24층이 적정한가, 아니면 무제한 + 하중이 자연 상한 역할을 하게 둘 것인가.
2. 엘리베이터를 어느 티어에 둘 것인가. 너무 이르면 동선 압박이 사라지고, 너무 늦으면 고층이 지루해진다.
3. 붕괴를 되돌릴 수 없게 할 것인가(하드코어) 아니면 보험 시스템을 둘 것인가.
4. 멀티 엔딩 분기 3안 중 어디까지 v1에 넣을 것인가.
5. UI 레이어 React 여부.
6. 세이브를 로컬 스토리지에만 둘 것인가, 내보내기/가져오기(JSON)를 지원할 것인가.
